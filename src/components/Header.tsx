import React, {useState} from 'react'

const Header = () => {

  const [nav, setNav] = useState(false)

  return (
    <header>
        <nav>
            
        </nav>
    </header>
  )
}

export default Header
